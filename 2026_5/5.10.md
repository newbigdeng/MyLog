## 5.10  
### 《程序员进阶之路》  
复习了缓存一致性协议MESI、讲解了MOESI、MESIF等各种处理器的变体实现。讲解了内存屏障(CPU在写数据时最先写入的是写缓冲区(store buffer)，等缓存行到位后再写入缓存行。收到无效化请求后也是，会先被放入无效队列(Invalid Queue)并返回ASK，内存屏障就是告诉CPU，把某些缓存里或队列里的操作处理完毕才执行后面的指令)，内存屏障指令分为四个(汇编指令)、而内存序(release、acquire、seq_cst这些)底层是不同的内存屏障指令组合实现的(也要看机器的架构)。以及介绍了CSA和无锁队列(MPMC版本)

### 《AI量化交易：智能体》
讲解了第二代权重提示词，角色、权重、背景、约束、输出实例。例子包括价值投资提示词、短线提示词。  
提及了skill式提示词  
自己实践了一下，把.yml格式的系统提示词扔给DeepSeek，帮我分析了几家公司。  
**深刻理解到知识储备丰富的人才能更好的使用ai帮助自己**

## 其他
了解了Coze的用法，工作流，明天准备搭建智能体