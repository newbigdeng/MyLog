## 5.14
### 《异步编程》
复习了并发(许多执行路径在重叠的时间段内，可能交错执行，就会发生并发)，并行(这些任务由不同的cpu单元执行，利用充足的多核资源，就会发生并行)  
介绍了几个编程范式，包括事件驱动编程、响应式编程、数据流编程。以及一些评估性能的公式。看完了第一章。

### 《程序员进阶之路》
复习了select/poll/epoll，select通过位图记录事件，poll通过结构体(描述符+监听类型)来记录事件，epoll通过epoll实例和epoll_event来记录事件。  
虚拟网卡：网卡将数据发给代理程序，解密并写入tun设备，发给应用层。发包：这个tun设备的ip作为源ip向某ip发包，内核转发给tun接口，包进入tun设备的驱动，vpn程序解析并处理这段数据，再发送给代理端口。

### 《高频交易系统面试题》

### 《精益创业》

### WonderTrader+AI解读  
StackTracer的Linux版本：使用backtrace获取调用函数的数量，并把地址写入字符串数组a，backtrace_symbols根据a，将具体模块、名称、偏移量、地址写入字符串数组b。后面就是分割每一个字符串，并将函数通过abi::__cxa_demangle将函数名还原成人类可读的形式，并格式化到日志(回调函数)里。  
看了WTSUtils的CMakeLists,这些工具最后会被编译成静态库  
SignalHook文件里面对各种信号进行了定义  
WTLMDB则是对lmdb的封装，包含了WtLMDB类和WtLMDBQuery类，有创建环境，打开/关闭数据库。以及事务管理，增删改查等操作。  
明天再把WTSCfgLoader等文件看完，完成WTSUtils的阅读，之后开始Include和WTSTools目录的阅读。

## 其他
买了两本书，《2+20：私募股权为什么能长期战胜市场》和《从创业到退出》。(我有一个创业之心)